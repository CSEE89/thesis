#include"SpectrumManager.h"
#include"TrafficMatrix.h"

#ifndef TRAFFIC_MANAGER
#define TRAFFIC_MANAGER
/*
struct TrafficLink{
	Path<ListGraph>  path;	
	int pos;
	int width;
	//int timestamp;
	PathMatrix(int k, Path<ListGraph>  &path, const int &pos, const int &width, const long int &timestamp) :unquie_key(k), path(path), pos(pos), width(width), timestamp(timestamp){}
	~PathMatrix()
	{
		path.clear();
	}
};
*/
/**
* GlobalSpectrumState egyes részit veszi át, 
* alloc-ot nem?
*/
/*
class TrafficManager{
	// BinaryTree or multiset<TrafficLink> 
	//TrafficMatrix traffic_matrix;
	//SpectrumManager spectrum_manager;
	std::multimap<int, PathMatrix> path_matrix;
public:
	void addLink(Link link){
		traffic_matrix.insert();
	}
	void removeLink(){}
	void check(){}

	void getend2endPaths(){}
};*/
#endif  TRAFFIC_MANAGER